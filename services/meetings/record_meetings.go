package meetings

import (
	"recordari/domain"
)

func PostMeetings(events []domain.Events) {

	/*getProjectsResponse := GetProjects().Body

	var projects []any

	err := json.NewDecoder(getProjectsResponse).Decode(&projects)
	if err != nil {
		log.Fatal("No se pudo descifrar los projectos obtenidos", err)
	}
	for _, project := range projects {
		println(project)
	}*/
	// http.Post(fmt.Sprint(HubplannerApiKey, "/timeentry"), "application/json", nil)
}
